---
---
<footer>
  <div class="wrap foot">
    <div class="brand">LearnFDE</div>
    <div class="small">learn-fde.com · learnfde.ai · 中文 / English</div>
  </div>
</footer>

<style>
  footer { border-top: 1px solid var(--glass-border); margin-top: 40px; padding: 40px 0 64px; }
  .foot { display: flex; justify-content: space-between; align-items: center; }
  .brand {
    font-family: var(--mono); font-size: 13px; font-weight: 700;
    background: linear-gradient(90deg, var(--violet), var(--cyan));
    -webkit-background-clip: text; background-clip: text; -webkit-text-fill-color: transparent;
  }
  .small { font-family: var(--mono); font-size: 11.5px; color: var(--faint); }
  @media (max-width: 640px) { .foot { flex-direction: column; gap: 12px; align-items: flex-start; } }
</style>
